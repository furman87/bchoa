%h1 Butler Creek Residents

%table.table.table-striped
    %thead
        %tr
            %th Name
            %th Address
            %th Email
            %th Phone
            %th Spouse Email
            %th Spouse Phone

    %tbody
        - @residents.each do |resident|
            %tr
                - if current_user.can_update?(resident)
                    %td= link_to resident.last_name_first, edit_user_registration_path
                - else
                    %td= resident.last_name_first
                %td= resident.address
                %td= resident.email
                %td= resident.phone_number
                %td= resident.spouse_email
                %td= resident.spouse_phone
